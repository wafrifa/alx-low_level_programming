#include <stidio.h>
/**
 * main - main is the entry for code
 * Returns: return 0 is used to exit the code
 */
int main(void)
{
  printf("with proper grammar, but the outcome is a piece of art,");
return (0);
}
